"""
    A package for installing a precompiled "gh" binary into axs environment.

    Usage examples :

        # get version:
            axs byquery extracted,gh , run --shell_cmd_with_subs='#{gh_path}# --version'

        # get help:
            axs byquery extracted,gh , run --shell_cmd_with_subs='#{gh_path}# --help'

        # any other command - just roll it yourself from the template above
"""
